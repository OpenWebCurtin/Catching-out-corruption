# On some systems installing pdfminer fails because chardet is unavailable.
# Install it first.
python -m pip install chardet

# Use Pip to install the pdfminer module.
python -m pip install pdfminer.six

